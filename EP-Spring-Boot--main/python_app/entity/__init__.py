"""SQLAlchemy ORM entity package.

Contains the ``Product`` model that maps to the ``product`` table in MySQL.
Translation of the Java ``entity/`` package which holds the JPA ``Product``
entity. The single member exported is ``entity.product.Product``.
"""
