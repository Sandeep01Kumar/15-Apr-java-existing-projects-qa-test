"""Pytest test suite for the Python (Flask) implementation.

This package mirrors the role of the Java side's
`src/test/java/com/jspider/spring_boot_simple_crud_with_mysql/`
package. It contains the context-load smoke test (`test_app.py`) and
shared fixtures (`conftest.py`).
"""
